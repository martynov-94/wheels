mixin page(page_title)
  doctype html
  html(lang="ru")
    head
      meta(charset="utf-8")
      meta(name="viewport" content="width=device-width, initial-scale=1.0")
      title= page_title
      link(rel='stylesheet', href='styles/project.css')
    body
      .wrapper
        +wrapper()